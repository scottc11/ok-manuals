import { LegendDefinition } from "../../../../types";

export const TOUCH_SEQUENCE: LegendDefinition = {
    label: "Touch Sequence",
    description: `Sequence of touch events which playback in real-time.`
}