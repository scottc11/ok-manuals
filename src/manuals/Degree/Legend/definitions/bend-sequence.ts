import { LegendDefinition } from "../../../../types";
import { BENDER } from "../bender";

export const BEND_SEQUENCE: LegendDefinition = {
    label: "Bend Sequence",
    description: `Sequence of ${BENDER.label} events which playback in real-time.`
}