import { LegendItem } from "../../../types"

export const GLOBAL_GATE_OUTPUT: LegendItem = {
    description: "Sums all Gate events into one output",
    label: "Global Gate Output",
    imageRef: 17,
    type: "output",
    voltageRange: "0V - +5V"
}