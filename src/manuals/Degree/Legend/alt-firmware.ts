import { LegendItem } from "../../../types";

export const ALT_FIRMWARE: LegendItem = {
    description: "A departure from the original firmware shipped with the DEGREE, this firmware handles sequencing differently.",
    label: "ALT Firmware",
    type: "firmware"
}